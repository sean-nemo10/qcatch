# Gemini API (sort / chat / AI features)
google-genai>=1.0.0

# Anthropic API (fallback for sort)
anthropic>=0.40.0

# Ollama local LLM (fallback for sort)
ollama>=0.4.0

# Structured output for sort
pydantic>=2.0.0

# Windows toast notification input
win11toast>=0.35

# PPTX generation (docs/generate_docs.py)
python-pptx>=1.0.0

# Google Calendar / Tasks integration
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Background scheduler for auto-sync
apscheduler>=3.10.0

# Build qcatch.exe
pyinstaller>=6.0.0
